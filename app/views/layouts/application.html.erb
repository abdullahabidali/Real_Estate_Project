<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "LevareEstates" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gradient-to-b from-blue-50 via-white to-purple-50 min-h-screen flex flex-col">
    <!-- Modern Header -->
    <nav class="w-full bg-white shadow-md py-4 px-8 flex justify-between items-center">
      <span class="text-2xl font-extrabold text-blue-700 tracking-wide">LevareEstates</span>
      <div class="flex space-x-4">
        <%= link_to "Home", root_path, class: "bg-blue-50 text-blue-700 hover:bg-blue-100 px-4 py-2 rounded-full font-semibold shadow transition" %>
        <%= link_to "Properties", properties_path, class: "bg-blue-50 text-blue-700 hover:bg-blue-100 px-4 py-2 rounded-full font-semibold shadow transition" %>
        <% if user_signed_in? %>
          <%= link_to "My Profile", profile_path, class: "bg-blue-50 text-blue-700 hover:bg-blue-100 px-4 py-2 rounded-full font-semibold shadow transition" %>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "bg-blue-50 text-blue-700 hover:bg-blue-100 px-4 py-2 rounded-full font-semibold shadow transition" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "bg-blue-500 text-white hover:bg-blue-600 px-4 py-2 rounded-full font-semibold shadow transition" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "bg-purple-500 text-white hover:bg-purple-600 px-4 py-2 rounded-full font-semibold shadow transition" %>
        <% end %>
      </div>
    </nav>

    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mx-auto mt-4 max-w-2xl" role="alert">
        <span class="block sm:inline"><%= notice %></span>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mx-auto mt-4 max-w-2xl" role="alert">
        <span class="block sm:inline"><%= alert %></span>
      </div>
    <% end %>

    <main class="flex-grow">
      <%= yield %>
    </main>

    <footer class="bg-gray-800 text-white mt-8">
      <div class="container mx-auto px-4 py-6">
        <div class="flex justify-between items-center">
          <div>
            <h3 class="text-lg font-semibold">LevareEstates</h3>
            <p class="text-gray-400">Find your dream property</p>
          </div>
          <div class="flex space-x-4">
            <a href="#" class="text-gray-400 hover:text-white">About</a>
            <a href="#" class="text-gray-400 hover:text-white">Contact</a>
            <a href="#" class="text-gray-400 hover:text-white">Privacy Policy</a>
          </div>
        </div>
      </div>
    </footer>

    <%= yield :scripts %>
  </body>
</html>
